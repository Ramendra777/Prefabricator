export default {
    content: [
        "./src/**/*.{js,ts,jsx,tsx}",
        // Include the path to your globals.css if using custom classes
        "./src/globals.css"
    ],
    theme: {
      extend: {},
    },
    plugins: [
      // Add plugins here if needed
    ],
  }